'use strict';

const reputexReactWidget = require('..');
const assert = require('assert').strict;

assert.strictEqual(reputexReactWidget(), 'Hello from reputexReactWidget');
console.info('reputexReactWidget tests passed');
